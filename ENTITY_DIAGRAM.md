# Entity Relationship Diagram (ERD)

## 📊 Complete Entity Relationship Model

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         ENTITY RELATIONSHIP DIAGRAM                          │
└─────────────────────────────────────────────────────────────────────────────┘

                                    ┌─────────────────────┐
                                    │   UserModelEntity   │
                                    │   (Users Table)     │
                                    ├─────────────────────┤
                                    │ PK: Id (Guid)       │
                                    │     Username (UK)    │
                                    │     Email (UK)      │
                                    │     PasswordHash    │
                                    │     Role            │
                                    │     CreatedAt       │
                                    └──────────┬──────────┘
                                               │
                                               │ 1
                                               │
                    ┌──────────────────────────┼──────────────────────────┐
                    │                          │                          │
                    │ (CreatedBy)              │ (CreatedBy)              │
                    │                          │                          │
                    │ *                        │ *                        │
                    ▼                          ▼                          ▼
        ┌──────────────────────┐    ┌──────────────────────┐
        │ CustomerModelEntity  │    │ SalesModelEntity     │
        │ (Customers Table)   │    │ (Sales Table)        │
        ├──────────────────────┤    ├──────────────────────┤
        │ PK: Id (Guid)        │    │ PK: Id (Guid)        │
        │     Name             │    │ FK: CustomerId       │
        │     Email            │◄───┤     Product          │
        │     Phone            │  * │     Quantity         │
        │     Address          │    │     Amount (decimal) │
        │     Company          │    │     SaleDate         │
        │     CreatedAt        │    │ FK: CreatedBy        │
        │     UpdatedAt        │    └──────────────────────┘
        │ FK: CreatedBy        │
        └──────────┬───────────┘
                   │
                   │ 1
                   │
                   │ (CustomerId)
                   │
                   │ *
                   ▼
        ┌──────────────────────────────┐
        │ CustomerTrafficModelEntity   │
        │ (CustomerTraffic Table)      │
        ├──────────────────────────────┤
        │ PK: Id (Guid)                 │
        │ FK: CustomerId (nullable)     │
        │     Source                    │
        │     Campaign                  │
        │     VisitDate                 │
        │     Page                      │
        └──────────────────────────────┘

LEGEND:
──────
PK = Primary Key
FK = Foreign Key
UK = Unique Key
*  = Many (One-to-Many relationship)
1  = One (Many-to-One relationship)
```

## 🔗 Relationship Details

### Relationship 1: User → Customer
```
User (1) ──────< (*) Customer
  │                    │
  │                    │
  │ CreatedBy          │ CreatedBy (FK)
  │                    │
  │                    │
  Relationship Type: One-to-Many
  Cardinality: 1 User can create Many Customers
  Delete Behavior: RESTRICT (Cannot delete User if Customers exist)
```

### Relationship 2: User → Sales
```
User (1) ──────< (*) Sales
  │                    │
  │                    │
  │ CreatedBy          │ CreatedBy (FK)
  │                    │
  │                    │
  Relationship Type: One-to-Many
  Cardinality: 1 User can create Many Sales
  Delete Behavior: RESTRICT (Cannot delete User if Sales exist)
```

### Relationship 3: Customer → Sales
```
Customer (1) ──────< (*) Sales
  │                      │
  │                      │
  │ Id (PK)              │ CustomerId (FK)
  │                      │
  │                      │
  Relationship Type: One-to-Many
  Cardinality: 1 Customer can have Many Sales
  Delete Behavior: RESTRICT (Cannot delete Customer if Sales exist)
```

### Relationship 4: Customer → CustomerTraffic
```
Customer (1) ──────< (*) CustomerTraffic
  │                         │
  │                         │
  │ Id (PK)                 │ CustomerId (FK, nullable)
  │                         │
  │                         │
  Relationship Type: One-to-Many
  Cardinality: 1 Customer can have Many Traffic records
  Delete Behavior: SET NULL (Traffic can exist without Customer)
```

## 📋 Entity Attributes

### UserModelEntity
```
┌─────────────────────────────────────────────────────────┐
│                    UserModelEntity                       │
├─────────────────────────────────────────────────────────┤
│ Primary Key:                                             │
│   • Id: Guid (auto-generated)                            │
│                                                          │
│ Attributes:                                              │
│   • Username: string(50) [Required, Unique]             │
│   • Email: string(100) [Required, Unique]               │
│   • PasswordHash: string(255) [Required]                │
│   • Role: string(50) [Required]                         │
│     Values: "Admin", "SalesManager", "SalesRep"         │
│   • CreatedAt: DateTime [Default: GETUTCDATE()]        │
│                                                          │
│ Relationships:                                           │
│   • Customers: ICollection<Customer> (via CreatedBy)    │
│   • Sales: ICollection<Sales> (via CreatedBy)          │
└─────────────────────────────────────────────────────────┘
```

### CustomerModelEntity
```
┌─────────────────────────────────────────────────────────┐
│                  CustomerModelEntity                     │
├─────────────────────────────────────────────────────────┤
│ Primary Key:                                             │
│   • Id: Guid (auto-generated)                           │
│                                                          │
│ Attributes:                                              │
│   • Name: string(100) [Required]                        │
│   • Email: string(100) [Optional]                       │
│   • Phone: string(20) [Optional]                        │
│   • Address: string(255) [Optional]                    │
│   • Company: string(100) [Optional]                    │
│   • CreatedAt: DateTime [Default: GETUTCDATE()]        │
│   • UpdatedAt: DateTime? [Optional]                    │
│                                                          │
│ Foreign Keys:                                            │
│   • CreatedBy: Guid [Required] → User.Id               │
│                                                          │
│ Relationships:                                           │
│   • User: UserModelEntity (Many-to-One)                │
│   • Sales: ICollection<Sales> (One-to-Many)            │
│   • Traffic: ICollection<CustomerTraffic> (One-to-Many)│
└─────────────────────────────────────────────────────────┘
```

### SalesModelEntity
```
┌─────────────────────────────────────────────────────────┐
│                   SalesModelEntity                      │
├─────────────────────────────────────────────────────────┤
│ Primary Key:                                             │
│   • Id: Guid (auto-generated)                           │
│                                                          │
│ Attributes:                                              │
│   • Product: string(100) [Required]                     │
│   • Quantity: int [Required]                           │
│   • Amount: decimal(18,2) [Required]                    │
│   • SaleDate: DateTime [Default: GETUTCDATE()]         │
│                                                          │
│ Foreign Keys:                                            │
│   • CustomerId: Guid [Required] → Customer.Id         │
│   • CreatedBy: Guid [Required] → User.Id               │
│                                                          │
│ Relationships:                                           │
│   • Customer: CustomerModelEntity (Many-to-One)        │
│   • User: UserModelEntity (Many-to-One)                │
└─────────────────────────────────────────────────────────┘
```

### CustomerTrafficModelEntity
```
┌─────────────────────────────────────────────────────────┐
│              CustomerTrafficModelEntity                  │
├─────────────────────────────────────────────────────────┤
│ Primary Key:                                             │
│   • Id: Guid (auto-generated)                           │
│                                                          │
│ Attributes:                                              │
│   • Source: string(50) [Required]                       │
│     Values: "Google", "Facebook", "LinkedIn", etc.      │
│   • Campaign: string(100) [Optional]                   │
│   • VisitDate: DateTime [Default: GETUTCDATE()]        │
│   • Page: string(200) [Optional]                       │
│                                                          │
│ Foreign Keys:                                            │
│   • CustomerId: Guid? [Optional] → Customer.Id         │
│     (Nullable - allows anonymous traffic)               │
│                                                          │
│ Relationships:                                           │
│   • Customer: CustomerModelEntity? (Many-to-One, Null) │
└─────────────────────────────────────────────────────────┘
```

## 🔄 Entity Lifecycle

```
┌─────────────────────────────────────────────────────────┐
│                    ENTITY CREATION FLOW                   │
└─────────────────────────────────────────────────────────┘

1. USER CREATION
   ┌──────────────┐
   │ Create User  │
   │ - Username   │
   │ - Email      │
   │ - Password   │
   │ - Role       │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Hash Password│
   │ (BCrypt)     │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Save to DB   │
   │ Generate Id  │
   └──────────────┘

2. CUSTOMER CREATION
   ┌──────────────┐
   │ Create       │
   │ Customer     │
   │ - Name       │
   │ - Email      │
   │ - CreatedBy  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Validate     │
   │ User exists  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Save to DB   │
   │ Generate Id  │
   └──────────────┘

3. SALE CREATION
   ┌──────────────┐
   │ Create Sale  │
   │ - CustomerId │
   │ - Product    │
   │ - Amount     │
   │ - CreatedBy  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Validate     │
   │ Customer &   │
   │ User exist   │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Save to DB   │
   │ Generate Id  │
   └──────────────┘

4. TRAFFIC CREATION
   ┌──────────────┐
   │ Create       │
   │ Traffic      │
   │ - Source     │
   │ - CustomerId │
   │   (optional)  │
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ Save to DB   │
   │ Generate Id  │
   └──────────────┘
```

## 📊 Database Schema

```
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE TABLES                             │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Users                                                            │
├──────┬──────────────┬──────────┬───────────────────────────────┤
│ Id   │ Username     │ Email    │ PasswordHash │ Role │ CreatedAt│
├──────┼──────────────┼──────────┼───────────────────────────────┤
│ PK   │ UK, NOT NULL │ UK, NOT  │ NOT NULL     │ NOT  │ DEFAULT  │
│      │              │ NULL     │              │ NULL │          │
└──────┴──────────────┴──────────┴───────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Customers                                                       │
├──────┬──────────┬──────────┬──────────┬──────────┬─────────────┤
│ Id   │ Name     │ Email   │ Phone    │ Address  │ Company     │
├──────┼──────────┼──────────┼──────────┼──────────┼─────────────┤
│ PK   │ NOT NULL │ NULL    │ NULL    │ NULL    │ NULL        │
│      │          │         │         │         │             │
├──────┬──────────┬──────────┬───────────────────────────────────┤
│ CreatedBy │ CreatedAt │ UpdatedAt                              │
├──────┼──────────┼──────────┼───────────────────────────────────┤
│ FK   │ DEFAULT  │ NULL                                         │
│      │          │                                              │
└──────┴──────────┴──────────┴───────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ Sales                                                           │
├──────┬──────────────┬──────────┬──────────┬───────────────────┤
│ Id   │ CustomerId   │ Product  │ Quantity │ Amount            │
├──────┼──────────────┼──────────┼──────────┼───────────────────┤
│ PK   │ FK, NOT NULL │ NOT NULL │ NOT NULL │ NOT NULL (decimal)│
│      │              │          │          │                   │
├──────┬──────────────┬──────────────────────────────────────────┤
│ SaleDate │ CreatedBy                                           │
├──────┼──────────────┼──────────────────────────────────────────┤
│ DEFAULT │ FK, NOT NULL                                        │
└──────┴──────────────┴──────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ CustomerTraffic                                                 │
├──────┬──────────────┬──────────┬──────────┬───────────────────┤
│ Id   │ CustomerId  │ Source   │ Campaign │ VisitDate         │
├──────┼──────────────┼──────────┼──────────┼───────────────────┤
│ PK   │ FK, NULL     │ NOT NULL │ NULL    │ DEFAULT           │
│      │              │          │         │                   │
├──────┬──────────────────────────────────────────────────────────┤
│ Page                                                            │
├──────┬──────────────────────────────────────────────────────────┤
│ NULL                                                            │
└──────┴──────────────────────────────────────────────────────────┘
```

## 🔐 Constraints & Indexes

```
PRIMARY KEYS:
─────────────
• Users.Id
• Customers.Id
• Sales.Id
• CustomerTraffic.Id

FOREIGN KEYS:
─────────────
• Customers.CreatedBy → Users.Id (RESTRICT on delete)
• Sales.CustomerId → Customers.Id (RESTRICT on delete)
• Sales.CreatedBy → Users.Id (RESTRICT on delete)
• CustomerTraffic.CustomerId → Customers.Id (SET NULL on delete)

UNIQUE CONSTRAINTS:
───────────────────
• Users.Username (Unique Index)
• Users.Email (Unique Index)

DEFAULT VALUES:
───────────────
• All Id fields: NEWID() (SQL Server GUID)
• CreatedAt fields: GETUTCDATE()
• SaleDate: GETUTCDATE()
• VisitDate: GETUTCDATE()

INDEXES:
────────
• Primary Key indexes (automatic)
• Foreign Key indexes (automatic)
• Unique indexes on Username and Email
```

